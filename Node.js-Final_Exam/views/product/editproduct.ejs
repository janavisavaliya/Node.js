<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Beauty Store - Dashboard</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        body {
            background-color: #fcf7ee;
        }

        .sidebar {
            width: 250px;
            height: 100vh;
            background: #ecdec1;
            color: white;
            padding: 20px;
            position: fixed;
        }

        .sidebar a {
            color: black;
            text-decoration: none;
            display: block;
            padding: 10px;
            border-radius: 5px;
            margin: 5px 0;
        }

        .sidebar a:hover {
            background: rgba(255, 255, 255, 0.2);
        }

        .content {
            margin-left: 270px;
            padding: 20px;
        }

        .card {
            border: none;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .form-container {
            background: white;
            padding: 40px;
            border-radius: 15px;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            max-width: 600px;
            margin: auto;
            text-align: center;
        }

        .form-container h2 {
            font-family: 'Playfair Display', serif;
            color: #d66a6a;
            font-weight: bold;
        }

        .btn-custom {
            width: 100%;
            padding: 12px;
            font-size: 16px;
            border-radius: 10px;
            transition: 0.3s;
            background-color: #ffb6c1;
            color: white;
            border: none;
        }

        .btn-custom:hover {
            background-color: #e695a0;
        }

        .form-control {
            border-radius: 10px;
            border: 1px solid #d8a7a7;
        }

        .btn-secondary {
            background-color: #f8d1c8;
            color: black;
        }

        .btn-secondary:hover {
            background-color: #e6b5b5;
        }

        .img-preview {
            border-radius: 10px;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
            margin-top: 10px;
        }
    </style>
</head>

<body>
    <div class="sidebar">
        <img src="https://wdtlilac.wpengine.com/wp-content/themes/lilac/assets/images/logo.svg" alt="" width="150px">
        <a href="/dashboard">Home</a>
        <a href="/product/add">Add Products</a>
        <a href="/product">View Products</a>
        <a href="/cart/viewcart">View Carts</a>
        <a href="/logoutuser">Logout</a>
    </div>


    <main class="content">
        <div class="header">
            <h2>Product Management</h2>
            <button class="btn btn-light">Search</button>
        </div>
        <div class="container mt-4">
            <div class="form-container">
                <h2 class="text-center">Update Beauty Product</h2>
                <form method="post" action="/product/update" enctype="multipart/form-data">
                    <input type="hidden" name="editid" value="<%= single?.id %>">

                    <div class="form-group row mb-3">
                        <label class="col-sm-3 col-form-label">Product Name</label>
                        <div class="col-sm-9">
                            <input type="text" name="product" value="<%= single?.product %>" class="form-control"
                                placeholder="Enter product name">
                        </div>
                    </div>

                    <div class="form-group row mb-3">
                        <label class="col-sm-3 col-form-label">Price</label>
                        <div class="col-sm-9">
                            <input type="number" name="price" value="<%= single?.price %>" class="form-control"
                                placeholder="Enter product price">
                        </div>
                    </div>

                    <div class="form-group row mb-3">
                        <label class="col-sm-3 col-form-label">Quantity</label>
                        <div class="col-sm-9">
                            <input type="number" name="qty" value="<%= single?.qty %>" class="form-control"
                                placeholder="Enter quantity">
                        </div>
                    </div>

                    <div class="form-group row mb-3">
                        <label class="col-sm-3 col-form-label">Description</label>
                        <div class="col-sm-9">
                            <textarea name="description" class="form-control" rows="3"
                                placeholder="Write product details"><%= single?.description %></textarea>
                        </div>
                    </div>

                    <div class="form-group row mb-3">
                        <label class="col-sm-3 col-form-label">New Image</label>
                        <div class="col-sm-9">
                            <input type="file" name="productImage" accept="image/*" class="form-control">
                            <% if (single?.productImage) { %>
                                <img src="<%= single?.productImage%>" width="100px" class="img-preview">
                                <% } %>
                        </div>
                    </div>

                    <button type="submit" class="btn btn-block btn-custom mt-3">💖 Update Product</button>
                    <a href="/product" class="btn btn-secondary btn-block btn-custom my-2">🔙 Back to Products</a>
                </form>
            </div>
        </div>
    </main>
</body>

</html>