<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        
        body {
            font-family: Arial, sans-serif;
            background-color: #faf3e049;
            color: #333333; 
            margin: 0;
            padding: 0;
            height: 100vh; 
            display: flex;
            justify-content: center;
            align-items: center; 
            text-align: center;
        }

        h1 {
            color: #ac7685; 
            text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.5);
            margin-top: 20px;
            animation: fadeIn 1s ease-out;
        }

        
        .form-container {
            width: 60%;
            padding: 20px;
            background-color: transparent; 
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
            animation: slideIn 1s ease-out; 
        }

        
        table {
            width: 80%;
            border-collapse: collapse;
            margin: 0 auto;
            animation: fadeIn 1.5s ease-out;
        }

        th, td {
            padding: 12px;
            text-align: left;
            color: #333333; 
        }

        tr {
            transition: background-color 0.3s ease;
        }

        tr:hover {
            background-color: #f2e1c628; 
        }

        td:first-child {
            width: 30%;
            font-weight: 400;
        }

        
        input[type="text"],
        input[type="datetime-local"] {
            width: 95%;
            padding: 8px;
            border: 1px solid #ac7685; 
            border-radius: 5px;
            background-color: #ac76852c; 
            color: #333333; 
            font-size: 14px;
            transition: all 0.3s ease-in-out; 
        }

        input[type="text"]:focus,
        input[type="datetime-local"]:focus {
            outline: none;
            border-color: #ac7685;
            box-shadow: 0 0 5px #ac7685;
            transform: scale(1.02); 
        }

        
        input[type="submit"] {
            padding: 10px 20px;
            background-color: #ac7685; 
            color: #FAF3E0; 
            font-size: 14px;
            font-weight: bold;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.3s ease;
            width: 100%; 
            margin-top: 10px; 
        }

        input[type="submit"]:hover {
            background-color: #ac7685; 
            transform: scale(1.05);
        }

        a {
            text-decoration: none;
            color: #ac7685; 
            font-weight: bold;
            margin-top: 20px;
            display: inline-block;
            transition: color 0.3s ease, transform 0.3s ease;
        }

        a:hover {
            color: #000; 
            transform: scale(1.1);
        }

        @keyframes fadeIn {
            0% {
                opacity: 0;
                transform: translateY(-30px);
            }
            100% {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes slideIn {
            0% {
                opacity: 0;
                transform: translateX(-100px);
            }
            100% {
                opacity: 1;
                transform: translateX(0);
            }
        }
    </style>
</head>
<body>
    <div class="form-container">
        <h1>Edit User</h1>
        <form action="/updateuser" method="post">
            <input type="hidden" name="editid" value="<%= singledata.id %>">
            <table border="1">
                <tr>
                    <td>Task</td>
                    <td>
                        <input type="text" name="usertask" value="<%= singledata.task %>">
                    </td>
                </tr>
                <tr>
                    <td>Status</td>
                    <td>
                        <input type="text" name="userstatus" value="<%= singledata.status %>">
                    </td> 
                </tr>
                <tr>
                    <td>Deadline</td>
                    <td>
                        <input type="datetime-local" name="userdeadline" value="<%= singledata.deadline %>">
                    </td>
                </tr>
                <tr>
                    <td></td>
                    <td>
                        <input type="submit" value="Update User">
                    </td>
                </tr>
            </table>
        </form>
        <a href="/">View Users</a>
    </div>
</body>
</html>
